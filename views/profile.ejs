<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile | SignalDesk</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700&family=Space+Grotesk:wght@500;600;700&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="page-shell">
        <header class="site-nav">
            <a class="site-brand" href="/">SignalDesk</a>
            <nav class="site-links">
                <a href="/">Dashboard</a>
                <a href="/profile" class="active">Profile</a>
                <a href="/settings">Settings</a>
            </nav>
            <div class="site-user">
                Signed in as <strong><%= user.fullName || user.username %></strong>
                <a class="nav-cta" href="/logout">Sign out</a>
            </div>
        </header>

        <section class="card">
            <div class="profile-hero">
                <div class="profile-avatar"><%= user.username.charAt(0).toUpperCase() %></div>
                <div class="profile-meta">
                    <h2><%= user.fullName || user.username %></h2>
                    <p>@<%= user.username %></p>
                    <p><%= user.email || 'Email not set' %></p>
                </div>
            </div>
        </section>

        <section class="card">
            <h3>Account Overview</h3>
            <div class="profile-grid">
                <div class="info-tile">
                    <label>Username</label>
                    <div><%= user.username %></div>
                </div>
                <div class="info-tile">
                    <label>Email</label>
                    <div><%= user.email || 'Not set' %></div>
                </div>
                <div class="info-tile">
                    <label>Full Name</label>
                    <div><%= user.fullName || 'Not set' %></div>
                </div>
                <div class="info-tile">
                    <label>Member Since</label>
                    <div><%= new Date(user.createdAt).toLocaleDateString() %></div>
                </div>
            </div>
        </section>

        <section class="card">
            <h3>Display Preferences</h3>
            <div class="profile-grid">
                <div class="info-tile">
                    <label>Watchlist</label>
                    <span class="status-pill <%= user.preferences && user.preferences.watchlist ? 'on' : 'off' %>">
                        <%= user.preferences && user.preferences.watchlist ? 'Enabled' : 'Disabled' %>
                    </span>
                </div>
                <div class="info-tile">
                    <label>Portfolio</label>
                    <span class="status-pill <%= user.preferences && user.preferences.portfolio ? 'on' : 'off' %>">
                        <%= user.preferences && user.preferences.portfolio ? 'Enabled' : 'Disabled' %>
                    </span>
                </div>
                <div class="info-tile">
                    <label>Screener</label>
                    <span class="status-pill <%= user.preferences && user.preferences.screener ? 'on' : 'off' %>">
                        <%= user.preferences && user.preferences.screener ? 'Enabled' : 'Disabled' %>
                    </span>
                </div>
                <div class="info-tile">
                    <label>News Feed</label>
                    <span class="status-pill <%= user.preferences && user.preferences.news ? 'on' : 'off' %>">
                        <%= user.preferences && user.preferences.news ? 'Enabled' : 'Disabled' %>
                    </span>
                </div>
                <div class="info-tile">
                    <label>Earnings Calendar</label>
                    <span class="status-pill <%= user.preferences && user.preferences.earnings ? 'on' : 'off' %>">
                        <%= user.preferences && user.preferences.earnings ? 'Enabled' : 'Disabled' %>
                    </span>
                </div>
                <div class="info-tile">
                    <label>Dark Mode</label>
                    <span class="status-pill <%= user.preferences && user.preferences.darkMode ? 'on' : 'off' %>">
                        <%= user.preferences && user.preferences.darkMode ? 'Enabled' : 'Disabled' %>
                    </span>
                </div>
            </div>
        </section>

        <section class="card">
            <h3>Next Steps</h3>
            <p>Update your preferences any time from the Settings page to tailor SignalDesk to your workflow.</p>
        </section>
    </div>
</body>
</html>
