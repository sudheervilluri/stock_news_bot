<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Settings | SignalDesk</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700&family=Space+Grotesk:wght@500;600;700&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="page-shell">
        <header class="site-nav">
            <a class="site-brand" href="/">SignalDesk</a>
            <nav class="site-links">
                <a href="/">Dashboard</a>
                <a href="/profile">Profile</a>
                <a href="/settings" class="active">Settings</a>
            </nav>
            <div class="site-user">
                Signed in as <strong><%= user.fullName || user.username %></strong>
                <a class="nav-cta" href="/logout">Sign out</a>
            </div>
        </header>

        <% if (message) { %>
            <div class="alert-banner success"><%= message %></div>
        <% } %>

        <section class="card">
            <h3>Display Preferences</h3>
            <p>Choose which surfaces appear in your dashboard. You can adjust these any time.</p>

            <form id="settingsForm" action="/settings" method="POST">
                <div class="settings-list">
                    <div class="settings-item">
                        <div>
                            <h4>Watchlist</h4>
                            <p>Track live quotes, technical posture, and quick fundamentals.</p>
                        </div>
                        <label class="toggle">
                            <input
                                type="checkbox"
                                name="watchlist"
                                value="true"
                                <%= user.preferences && user.preferences.watchlist ? 'checked' : '' %>
                            >
                            <span class="toggle-slider"></span>
                        </label>
                    </div>

                    <div class="settings-item">
                        <div>
                            <h4>Portfolio</h4>
                            <p>Monitor P&L, cost basis, and exposure in real time.</p>
                        </div>
                        <label class="toggle">
                            <input
                                type="checkbox"
                                name="portfolio"
                                value="true"
                                <%= user.preferences && user.preferences.portfolio ? 'checked' : '' %>
                            >
                            <span class="toggle-slider"></span>
                        </label>
                    </div>

                    <div class="settings-item">
                        <div>
                            <h4>Screener</h4>
                            <p>Filter your tracked symbols by momentum and volume.</p>
                        </div>
                        <label class="toggle">
                            <input
                                type="checkbox"
                                name="screener"
                                value="true"
                                <%= user.preferences && user.preferences.screener ? 'checked' : '' %>
                            >
                            <span class="toggle-slider"></span>
                        </label>
                    </div>

                    <div class="settings-item">
                        <div>
                            <h4>Market Feed</h4>
                            <p>Stay ahead with a unified stream of relevant headlines.</p>
                        </div>
                        <label class="toggle">
                            <input
                                type="checkbox"
                                name="news"
                                value="true"
                                <%= user.preferences && user.preferences.news ? 'checked' : '' %>
                            >
                            <span class="toggle-slider"></span>
                        </label>
                    </div>

                    <div class="settings-item">
                        <div>
                            <h4>Earnings Calendar</h4>
                            <p>Track results and calls for the symbols you follow.</p>
                        </div>
                        <label class="toggle">
                            <input
                                type="checkbox"
                                name="earnings"
                                value="true"
                                <%= user.preferences && user.preferences.earnings ? 'checked' : '' %>
                            >
                            <span class="toggle-slider"></span>
                        </label>
                    </div>
                </div>

                <h3 class="section-spacer">Appearance</h3>
                <div class="settings-list">
                    <div class="settings-item">
                        <div>
                            <h4>Dark Mode</h4>
                            <p>Enable low-light styling for evening research sessions.</p>
                        </div>
                        <label class="toggle">
                            <input
                                type="checkbox"
                                name="darkMode"
                                value="true"
                                <%= user.preferences && user.preferences.darkMode ? 'checked' : '' %>
                            >
                            <span class="toggle-slider"></span>
                        </label>
                    </div>
                </div>

                <div class="form-actions">
                    <button type="submit" class="primary">Save Preferences</button>
                </div>
            </form>
        </section>

        <section class="card">
            <h3>Account Details</h3>
            <div class="profile-grid">
                <div class="info-tile">
                    <label>Username</label>
                    <div><%= user.username %></div>
                </div>
                <div class="info-tile">
                    <label>Email</label>
                    <div><%= user.email || 'Not set' %></div>
                </div>
                <div class="info-tile">
                    <label>Member Since</label>
                    <div><%= new Date(user.createdAt).toLocaleDateString() %></div>
                </div>
            </div>
        </section>
    </div>

    <script>
        document.getElementById('settingsForm').addEventListener('submit', function() {
            const checkboxes = document.querySelectorAll('input[type=\"checkbox\"]');
            checkboxes.forEach((checkbox) => {
                if (!checkbox.checked) {
                    checkbox.removeAttribute('name');
                }
            });
        });
    </script>
</body>
</html>
