<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SignalDesk | Overview</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700&family=Space+Grotesk:wght@500;600;700&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="page-shell">
        <header class="site-nav">
            <a class="site-brand" href="/">SignalDesk</a>
            <nav class="site-links">
                <a href="/" class="active">Overview</a>
                <a href="/profile">Profile</a>
                <a href="/settings">Settings</a>
            </nav>
            <div class="site-user">
                Signed in as <strong><%= user.fullName || user.username %></strong>
                <a class="nav-cta" href="/logout">Sign out</a>
            </div>
        </header>

        <section class="card">
            <h2>Market Snapshot</h2>
            <p>SignalDesk keeps your tracked symbols, news, and upcoming events aligned in one workspace.</p>
        </section>

        <section class="card">
            <h3>Tracked Stocks</h3>
            <form action="/add-stock" method="post" class="inline-form">
                <input type="text" name="stockSymbol" placeholder="Add a stock symbol">
                <button type="submit" class="primary">Add</button>
            </form>
            <div class="stack-spacing">
                <ul class="list-stack">
                    <% stocks.forEach(stock => { %>
                        <li class="list-item">
                            <div class="list-item-title"><%= stock %></div>
                            <form action="/delete-stock" method="post" class="inline-form">
                                <input type="hidden" name="stockSymbol" value="<%= stock %>">
                                <button type="submit" class="danger">Remove</button>
                            </form>
                        </li>
                    <% }) %>
                </ul>
            </div>
        </section>

        <section class="card">
            <h3>Latest News</h3>
            <ul class="list-stack">
                <% news.forEach(article => { %>
                    <li class="list-item">
                        <div>
                            <div class="list-item-title"><%= article.title %></div>
                            <div class="list-item-meta"><%= article.source.name %></div>
                        </div>
                        <a class="text-link" href="<%= article.url %>">Read more</a>
                    </li>
                <% }) %>
            </ul>
        </section>

        <section class="card">
            <h3>Upcoming Earnings</h3>
            <ul class="list-stack">
                <% upcomingResults.forEach(result => { %>
                    <li class="list-item">
                        <div>
                            <div class="list-item-title"><%= result.stock %> — <%= result.date %></div>
                            <div class="list-item-meta">EPS: <%= result.reportedEPS %> (reported) | <%= result.estimatedEPS %> (estimate)</div>
                        </div>
                    </li>
                <% }) %>
            </ul>
        </section>
    </div>
</body>
</html>
